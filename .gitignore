# Ignore nested git repositories inside src (keep their files)
# Prevent accidentally committing nested .git directories (e.g. src/minilibx-linux/.git)
src/*/.git/

# Common build artifacts (optional, keep if helpful)
obj/
*.o
cub3D
cub3B
cub3M