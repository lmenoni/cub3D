# Ignore nested git repositories inside src/ (keep their files)
# This prevents accidentally committing nested .git directories (e.g., src/minilibx-linux/.git, src/my_libft/.git)
src/*/.git/

# Common build artifacts
obj/
*.o
cub3D
cub3B
cub3M
texture